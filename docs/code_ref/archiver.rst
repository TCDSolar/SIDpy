Verify and generate archive structure, specify paths of newly processed files
and archive them accordingly. The default structure of the archive is
{site}/YYYY/MM/DD/{file_type}/.

SIDpy Archiver
**************

The ``archiver`` verifies and generates the archive structure, specifying the paths of newly processed files
and archive them accordingly. The default structure of the archive is {site}/YYYY/MM/DD/{file_type}/.

.. automodapi:: sidpy.archiver
